# 项目规范

## 强制前置检查：读取 mistake.txt

**每次修改代码之前**，必须先读取 `mistake.txt`，了解已记录的历史错误，确保不重复犯错。

读取后对照当前修改内容，逐项检查：
1. 本次改动是否涉及 mistake.txt 中记录的同类模式（模板替换、字符串匹配、构建产物验证等）
2. 如果涉及，采用该文件中记录的正确做法
3. 修改完成后，用构建/测试验证产物是否符合预期

## 构建验证

- 重构 `index.html` 后，检查产物中是否残留未编译的 JSX（搜索 `return\s*\(\s*<[a-zA-Z]`）
- `--no-babel` 构建后，检查产物是否包含 `text/babel` 和 `babel.min.js`
- 生产构建后，检查产物**不应**包含 `text/babel` 或 `babel.min.js`

## mistake.txt 维护

- 发现新的坑或踩过的错，追加到 `mistake.txt`
- 格式：日期 + 错误现象 + 根本原因 + 修复方案 + 教训
