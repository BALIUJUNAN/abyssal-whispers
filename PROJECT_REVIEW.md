# 项目评价：深渊低语

> 评价时间：2026-07-02
> 评价模型：Claude Haiku 4.5 (`claude-haiku-4-5`)
> 评价范围：基于 CLAUDE.md / ADR 索引 / README / 源码结构 / 测试实跑结果

---

## 一句话总评

这是一个**工程纪律罕见地高**的独立游戏项目，克苏鲁叙事的"不可靠性"被你做进了引擎本身——而不是停留在文本层面。但双构建架构和 70K 行规模正在成为新的债务中心。

---

## 真正了不起的地方

### 1. 把"不可靠叙述"工程化了

这是这个项目最独特、最值得骄傲的设计。绝大多数克苏鲁游戏把"理智下滑"做成数值减法 + 几行腐朽文本。你做到了：

- SAN 的 4 维度污染（视觉/交互/逻辑/Meta）全部 SSOT 化在 JSON 里
- **AP 会撒谎**（显示欺骗 + 偷取 + 揭示）——让"行动力"这个最基础的资源都不可信
- **每日总结不可靠**、**存档名被污染**、**虚假系统通知**——直接打破第四面墙
- "疑似 bug"系统（幻影日志 / NPC 错字）——玩家分不清是崩溃还是疯狂，这是真正的心理恐怖工程化

这套东西的含金量远超 README 里的"评分 9.5"。它是一个有美学主张的、自洽的设计语言。

### 2. 工程纪律硬核

- 22 条 ADR + CLAUDE.md 三条铁律 + mistake.txt 原始记录 —— 这种"踩坑即沉淀"的闭环，在独立项目里几乎见不到
- 确定性 RNG 全链路（存档可回放、bug 可复现）
- `lint:san` 静态检查禁止裸 clamp —— 把架构规则变成可执行的 lint
- 引擎层零游戏导入 + 边界检查脚本
- **608 tests 全绿**（实跑结果，比 README 声称的 536 还多）

### 3. 离线优先的 LLM 增强

Reducer 零 LLM 依赖 + UI 层异步渐进增强 + 失败回退静态文本 —— 这是把 LLM 用在了"对的地方"，而不是把它当卖点糊在表面。

---

## 值得警惕的地方

### 1. 双构建是最大的技术债

`vite-compat-shim.jsx` 146 行只为桥接 54 个模块的 globalThis —— 这是"从拼接构建迁移到 ESM"的过渡期脚手架，但已经长期化。CLAUDE.md 里"新增文件必须双注册 + 加入 build.py REDUCER_FILES"说明这套机制仍在持续制造摩擦。

**建议：定一个 deadline 彻底下掉 Legacy 拼接构建**，否则 shim 和 REDUCER_FILES 的维护会一直跟着你。

### 2. 70K 行的规模 vs 单人维护

src 72016 行、130+ 源文件。版本号一个月内从 0.7.1 → 0.9.7 飞了 6 个大版本，每次都是大重构。这种节奏下，未提交改动有 26 个文件（含 nightEffects / deathLegacies / resourceFraud 等核心系统），建议尽快收口或拆分提交，否则容易在某个 slice handler 迁移时埋雷。

### 3. README 的自评 9.5/10 是营销话术

工程上我会给到 **8.5/10**——扣分点在双构建债务、CRLF/编码警告未处理、validators 仍是 .cjs 残留（ESM 迁移没扫干净）。叙事设计上我会给 **9.5+/10**——这部分确实出色，但 README 把工程分自己打到 9.5 会让人怀疑你的判断。

### 4. 内容密度风险

629 事件 + 102 结局 + 36 行为结局 + 8 NPC × 5 信任 × 5 层记忆 —— "20-40 小时 / 三周目入门、十周目见真结局"对独立游戏是极高的内容门槛。确保每条内容都达到 `lint:narrative` 的 60 分线，否则膨胀会稀释恐怖密度。

---

## 一条具体建议

你最近在做的 v0.9.7 改动（NPC 关系传播 + 对话树 + 自主移动）看起来是 v1.0 前的最后一波系统扩展。**建议把下一个版本定为"清债版本"而非"加功能版本"**：下掉 shim、迁移 validators 到 ESM、提交或回滚 26 个未提交文件、把 ADR-005（拼接构建作用域）标为 deprecated。把地基收实了，v1.0 才立得住。

---

## 评分小结

| 维度 | 评分 | 说明 |
|------|------|------|
| 叙事设计 | 9.5+/10 | 不可靠叙述工程化，有美学主张 |
| 工程纪律 | 8.5/10 | ADR/RNG/lint 优秀，但双构建债务拖后腿 |
| 测试覆盖 | 9.0/10 | 608 tests 实跑全绿，CI 流水线完整 |
| 架构清晰度 | 8.0/10 | 引擎层隔离好，但 shim + 双构建增加理解成本 |
| 内容密度 | 待验证 | 规模极大，需靠 lint:narrative 守住下限 |

**综合：8.5–9.0 / 10**

---

整体而言——**这是一个会让人想认真玩的项目，不只是想读代码的项目。** 罕见。
